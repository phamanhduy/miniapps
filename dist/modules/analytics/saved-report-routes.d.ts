/**
 * saved-report-routes.ts — CRUD for SavedReport + run saved config.
 * All routes require JWT auth, scoped to user's orgId.
 */
import type { FastifyInstance } from 'fastify';
export declare function savedReportRoutes(app: FastifyInstance): Promise<void>;
//# sourceMappingURL=saved-report-routes.d.ts.map